# OpenAI Token Manager fork

- 上游：xiaopenghuang/Token-Manager；fork：blackdm666/Token-Manager。
- 本地独立仓库；维护分支codex/functional-improvements，origin为fork，upstream为上游。
- 保留原项目名称、LICENSE和作者署名。运行数据tokens/、outputs/、token_manager_config.json和构建dist/均不入Git。
- Sub2API业务配置使用官方管理API，服务器规则仍遵守工作区AGENTS。生产只读验收可使用sub2api-maintenance/AGENTS.md中的凭据读取方式，禁止复制真实Key或账号token到源码、测试、日志、报告或打包产物。
- 上传配置：并发、优先级、倍率、proxy_id、group_ids、extra.codex_fingerprint_mode；WS默认ctx_pool，对应extra.openai_oauth_responses_websockets_v2_mode及enabled。以Sub2API运行版本API契约为准，不自行编造字段。
- 2.2.0-88api.2起已移除CPA功能，勿恢复CPA界面/上传/导出依赖；历史磁盘文件不得因代码清理而删除。分组选择使用CheckList复选框，支持多选。
- 2.2.0-88api.5起本地proxy_id配置兼容逗号分隔/列表代理池，服务端API仍只提交单个proxy_id。新账号随机分配并保存本地assignment，重试沿用，远端已有池内代理优先保留；自动401恢复不改代理。切勿把代理ID列表直接发送到Sub2API的proxy_id字段。
- 主界面固定保留本地凭据与右侧工作区双栏，不提供隐藏本地栏的“远端 / 双栏”切换；大标题已移除，运行日志固定显示在底部。
- 2.2.0-88api.6右侧默认及重置筛选均oauth；排序显式请求name/asc并保留服务端顺序，本地已匹配账号跟随远端顺序。右侧无嵌套列表页签与身份详情占位，账号操作在菜单，用量明细双击查看底部页签。
- 5h/7d用量先读账号extra里的codex快照；手动更新最多50账号，官方usage/batch每批20、force=false（服务器可能按自身规则刷新过期快照）。缺失不是0，过期显示待更新。禁止估算剩余Token；不能将列表账号名排序等同于用户浏览器保存的任意自定义排序。
- 2.2.0-88api.8覆盖上述旧版排序/布局描述：默认ID数值升序，右侧表头可升降序，用量列按数字排序，暂无项放后。日志常显且无用量明细页签，清空按钮浮于日志角落；维护规则在右侧导航行，本地统计在标题旁。使用UsageTreeview为可见额度单元格绘制进度条。已配置连接启动后读列表，每60秒同步服务器已有快照（GET账号列表、不调用批量用量），自动维护运行时复用其状态轮询；手动更新用量仍显式调用usage/batch。
- 2FA返回兼容JSON会话与Cookie、本地RFC6238生成验证码；不得改为将2FA密匙发送给第三方网站，不绕过Cloudflare/CAPTCHA等安全检查，异常必须明确阶段和处理建议。
- 账号自动恢复必须显式开启，限制到已导入本地且唯一匹配的OpenAI OAuth账号。普通429、主动停用、临时停调度不自动恢复。永久撤销需重新授权，不尝试绕过登录或封禁。
- 恢复使用apply-oauth-credentials官方入口更新原账号，不新建/删除账号，不覆盖原分组/并发/代理/指纹。必须检查工作区身份、并发凭据变化、读回结果；本地刷新结果先落盘，网络失败重试上传，不反复旋转refresh token。
- 回归命令：`python -m unittest discover -s tests -v`、`python -m compileall -q token_manager tests`、`git diff --check`。GUI测试使用临时目录和合成账号，禁止载入生产凭据。
- 打包：`python build.py --name OpenAI-Token-Manager-88API --entry-point main.py`，无需`--clean`；不删除用户已有dist内运行数据。标准打包成功后自动复制为桌面版本号EXE，并只删除桌面上匹配`88API-号池自动维护工具-v*.exe`的旧版本；需要只保留dist时显式添加`--no-desktop-install`。
- GitHub发布、全量生产自动恢复需依据当次用户授权，不因为凭据可用而自行启用。

- 2.2.0-88api.9：任务忙碌或自动维护运行时，重复点击刷新等操作仅记录日志提示，不再弹出阻塞提示框；禁止在任务锁内调用Tk弹窗或其他UI回调，避免定时器重入死锁。

- 2.2.0-88api.10经用户明确授权扩展自动维护：401/永久撤销可使用CredentialVault中的账号密码与TOTP重新登录，当前Windows用户DPAPI加密文件位于系统文档/OpenAI-Token-Manager/credentials/accounts.dpapi。仅处理已显式监控，或符合自动纳入规则且唯一匹配的本地账号；新凭据严格校验邮箱/工作区/有效期后落盘，再用原账号apply-oauth-credentials补授权。自动登录不写诊断报告，不绕过登录挑战。缺资料、挑战、身份不符或超过重试限制转人工。
- Sub2API设置新增自动维护页：auto_reauthorize_401/recovery_test_enabled默认true，recovery_test_model默认gpt-5.5（已用#384原生测试验证）。补授权后先GET核验active/schedulable/冷却，再POST accounts/:id/test解析SSE test_complete.success；每新凭据至多一次，持久化测试开始避免崩溃后重复计费。429/主动停用不触发登录，测试失败不等同授权失败。人工停用不自动启用。普通轮询仅GET不发模型请求。
- 凭据资料只在2FA导入、点击加密保存、启动批量授权或正常退出时保存；启动只显示资料数量，点击载入才解密显示。旧版本未持久化的输入不可自动恢复，需首次导入。测试必须patch资料库或使用临时路径，不能加载真实资料；禁止为测试人为制造生产401。

- 生产接口注意：列表与详情会隐藏OAuth Token。普通轮询显示“凭据已隐藏”，不误报不一致；仅恢复阶段通过官方GET accounts/data?ids=<单账号ID>&include_proxies=false读取凭据，在内存校验身份/并发变化/补授权读回，不落盘导出文件。接口403时停止恢复，不绕过二次验证。实测当前管理员接口可用。

- Sub2API列表默认筛选分组ID2（精确按ID，不依赖分组名称）。在“Sub2API设置→列表显示”编辑默认列表分组ID，支持逗号分隔多选，留空显示全部；启动、重置筛选和修改保存后应用。临时多选不改变默认配置。此字段default_list_group_ids独立于上传group_ids及监控范围。

- 2.2.0-88api.11：Sub2API列表在状态右侧增加“调度”列，可点击排序。依据schedulable、status及冷却截止时间显示参与调度/已关闭/开启·账号停用或异常/开启·临时、限流、过载冷却；未知不当作开启。调度表示服务器快照中的参与资格，不代表当前正在处理请求；随列表轮询更新，不修改远端调度开关。

- 2.2.0-88api.12：自动维护默认纳入已有Sub2API成功上传且唯一匹配的本地账号，不再要求先点击监控选中；“取消监控”写入manual_disabled，保持人工排除。补授权恢复或待验证阶段若账号已active但schedulable=false，且auto_enable_schedulable开启，调用官方POST /accounts/:id/schedulable true并读取确认，再进行模型测试；普通健康轮询不得重新开启人工关闭的调度。调度未知不当作参与调度。

- 2.2.0-88api.13：Sub2API账号操作菜单只提供“启用调度/停用调度”，调用官方POST /accounts/:id/schedulable；不再使用bulk-update status。status=active/inactive仅由账号状态操作或服务端返回决定。核对#333、#342时官方列表与详情均为active、schedulable=true、无冷却。

- 2.2.0-88api.14：右侧Sub2API列表使用官方accounts?lite=1读取current_concurrency和concurrency，每5秒同步；current_concurrency是当前请求占用计数，不等同active_sessions（OpenAI通常为空）。额度展示移除5h，仅保留7d。左侧身份标签按Sub2API plan_type：Plus、Pro 20x、Pro 5x、Business Standard、Business Premium、Free、Enterprise、Unknown。

- 2.2.0-88api.15：右侧Sub2API列表原“分组”列改为“账号标签”，标签按OpenAI plan_type定义并优先使用本地匹配账号的Sub2API标签。账号操作菜单统一称“刷新令牌（选中）”；该动作调用Sub2API POST /api/v1/admin/accounts/batch-refresh，刷新远端OAuth令牌，不是刷新列表或额度。

- 2.2.0-88api.16：Sub2API列表账号行支持右键菜单，右键行会自动选中，提供启用调度、停用调度、刷新令牌、删除选中；刷新令牌调用官方batch-refresh接口。


## 2.2.0-88api.17：智能补授权与2FA资料删除

- 2FA页的“启动批量授权”改为“智能补授权”。每次点击后在后台重新读取Sub2API账号列表，按邮箱/工作区/绑定ID唯一匹配；远端active且没有明确缺失凭据时跳过。调度关闭、额度冷却不等于授权失效。inactive、非401异常或身份歧义跳过并记录原因；网络读取失败终止本批，不回退全量登录。
- 明确401/授权撤销/缺少凭据或首次添加的账号才进入授权。已有未过期新凭据处于待上传/待验证阶段时提示继续同步，不重复授权。未匹配远端但已有本地未过期凭据时先提示核对上传。状态是检查时刻的Sub2API快照，不是逐账号真实模型探测。
- 结果展示成功、失败、跳过数量，每个跳过原因写入日志。正常账号不会调用登录、MFA或刷新令牌接口。首次使用需先配置Sub2API连接，以便进行状态核对。
- 删除入口：2FA → 管理已存资料 → 搜索邮箱/多选 → 删除所选资料 → 确认。列表只显示邮箱，不展示密码或密匙。删除使用DPAPI密文原子替换，保留其他账号；同时移除输入框对应行，避免退出时再次保存。任务或维护运行时禁止删除资料；不会删除本地Token文件或Sub2API账号。若需要以后重新授权被删账号，须重新导入资料。
- “清空输入”只清空编辑框，与删除资料库是两个动作。不要自动删除用户的生产账号；回归删除测试仅使用临时资料库。

- 2.2.0-88api.18：自动维护和智能补授权允许同一 ChatGPT 工作区在 Sub2API 远端删除重建后自动更新绑定 ID；只有旧绑定仍存在、工作区不一致或身份重复时才停止，不放宽手动上传的严格绑定检查。
- 2.2.0-88api.19：Sub2API账号操作菜单和右键菜单统一显示“刷新令牌”，仍只作用于当前选中账号。
- 2.2.0-88api.20：移除左侧刷新当前、上传当前和取消监控按钮；本地账号右键菜单保留刷新本地令牌、上传到 Sub2API、监控/取消监控和删除账号。
- 2.2.0-88api.21：左侧按钮命名为“重新读取账号”和“刷新本地令牌”；右侧 Sub2API 按钮继续命名为“刷新列表”。
- 2.2.0-88api.22：左侧账号操作按三列三行 3-3-3 九宫格排列。
- 2.2.0-88api.23：左侧三列布局中的“上传配置”按钮改为“远端配置”，仍进入 Sub2API 设置。
- 2.2.0-88api.24：本地账号列表统一称为“本地凭据”；Sub2API底部统计显示当前筛选账号数量、可用账号、并发请求和并发上限，移除旧服务器快照统计与重置筛选按钮。
- 2.2.0-88api.25：调度列中参与调度且限流的账号显示“限流中”；schedulable=false 的限流账号仍显示“已关闭”。
- 2.2.0-88api.26：调度列使用“调度中”；Sub2API用量列后增加优先级；右键支持编辑远端运营参数；远端代理池支持下拉多选。该版本曾让本地授权随机使用远端代理端点，此行为已被2.2.0-88api.48废止。

- 2.2.0-88api.27：Sub2API账号标签、状态、调度、并发、7d已用、优先级列内容居中显示。

- 2.2.0-88api.28：智能补授权自动从DPAPI资料库按邮箱读取首次保存的密码和2FA密钥；输入框为空也能运行，不回填敏感资料。

- 2.2.0-88api.29：左侧九宫格操作按钮增加悬停功能说明。

- 2.2.0-88api.30：智能补授权以左侧本地凭据列表为唯一准入范围；本地账号删除后即使DPAPI资料仍在也视为废弃并跳过。

- 2.2.0-88api.31：自动授权运行中按钮切换为停止自动授权；停止可中止本地OAuth回调等待并结束任务。

- 2.2.0-88api.32：修复 Tokens 目录误指向整个文档目录导致启动扫描卡死；启动时自动迁移到专用目录，优先保留已有桌面 tokens 数据。

- 2.2.0-88api.33：导入导出页移除重复的 Sub2API 占位内容并将四个主要按钮整理为一排；远端账号支持多选后统一编辑运营参数，空白字段保持原值。

- 2.2.0-88api.34：本地凭据、导出文件和配置统一使用 `C:\Users\Administrator\Documents\sub2api`；兼容旧桌面目录并迁移加密 2FA 资料到 outputs。

- 2.2.0-88api.47：Sub2API账号标签读取官方DTO的`credentials.plan_type`，并兼容顶层、父账号、extra与workspace字段；从Sub2API导入时保留套餐标签，本地标签未知时可用唯一匹配远端标签回退。Free只表示套餐身份，不能替代401、429、调度和模型测试等存活判断。
- 2.2.0-88api.48：OAuth、2FA协议链/浏览器链、本地令牌刷新和401自动重新授权统一使用软件独立`auth_proxy`；留空直连。严禁从Sub2API `proxy_id`代理池读取端点用于本地授权。`http_proxy`仅作为软件接口连接代理，远端账号业务代理仍由Sub2API `proxy_id`独立管理。
- 2.2.0-88api.49：`authsess_*`只是一次登录会话ID，不能当作稳定ChatGPT工作区ID。加载本地凭据时优先使用JWT内`chatgpt_account_id`，远端临时会话ID不参与工作区冲突判断，避免成功授权后被智能补授权误报“同邮箱工作区不一致”。
- 2.2.0-88api.50：授权代理只在“设置→OAuth”维护，2FA页不再重复显示。Sub2API设置窗口使用较短默认高度和固定底部操作区，确保高DPI/小屏环境下读取、保存、取消按钮始终可见。
- 2.2.0-88api.51：授权代理移动到“设置→基础”，紧邻“软件接口代理”，并在标签中明确一个只用于Sub2API管理接口、一个只用于OAuth/2FA。若仅填写软件接口代理而授权代理为空，启动2FA前必须记录警告并继续按直连处理，不能静默误导为已使用代理。
- 2.2.0-88api.52：智能补授权完成后必须在软件内逐账号展示成功、失败、跳过和输入错误；失败/跳过原因及诊断报告路径直接显示在脱敏详情区，并支持复制。外部JSON继续保留审计用途，但不能作为唯一查看入口。
- 2.2.0-88api.53：标准打包成功后自动原子复制当前版本到Windows桌面，再删除桌面上同命名规则的旧版本号EXE；不得递归扫描或删除其他桌面文件。旧版本仍在运行时先对其窗口发送正常关闭请求，让程序执行自身保存/任务保护逻辑；仍无法关闭或删除时必须明确报错，禁止强制结束正在写凭据的任务。
- 2.2.0-88api.54：2FA页“添加到左侧凭据”只创建带`metadata.auth2fa_pending`的本地占位记录，密码和TOTP密匙仍只能在DPAPI加密资料库中保存；左侧状态显示“待授权”，完成OAuth后由TokenStore清除占位标记。待授权记录禁止刷新、上传空凭据或生成上传预览。
- 2.2.0-88api.55：智能补授权成功后始终保存新OAuth凭据并立即重新读取左侧本地凭据；手动上传到Sub2API成功后自动刷新远端账号快照，避免继续显示旧的过期/401状态。
- 2.2.0-88api.56：左侧本地凭据状态统一显示为“正常/失效”；调度关闭、冷却等运营状态不再混入凭据状态，详细信息仍保留在账号详情和Sub2API列表。
- 2.2.0-88api.57：增加云更新框架：HTTPS更新清单、下载包SHA-256校验、独立更新进程安全替换EXE；更新只操作程序文件，不触碰Tokens、配置和DPAPI资料。
- 2.3.0：版本号改为纯Vx.x.x格式；云更新地址固定为test.88api.ai，启动时自动检查；更新包由独立更新进程安全替换桌面旧版本，分发目录与test.88api.ai原有程序隔离。

## 自动上号器维护、发布与云更新规则（2.3.0起）

### 一、项目与版本号

- 本项目即“88API号池自动维护工具/自动上号器”，代码目录为 `openai-token-manager/`；不要把桌面客户端改动混入 Sub2API 或 NewAPI 服务端仓库。
- 当前版本号采用纯语义版本 `x.y.z`，代码和 `version.json` 中填写不带前缀的值，例如 `2.3.1`；界面、命令行和桌面文件名可显示为 `v2.3.1`。
- 禁止继续使用 `2.2.0-88api.58` 之类内部流水号作为新版本号。历史 changelog 和兼容测试中的旧版本号可以保留，但不能作为当前发布版本。
- 修复问题递增 patch（例如 `2.3.1`），新增兼容功能递增 minor（例如 `2.4.0`），重大不兼容修改递增 major（例如 `3.0.0`）。
- 每次发布必须同步修改：
  - `token_manager/constants.py` 的 `APP_VERSION`；
  - `version.json` 顶层 `version`；
  - `version.json` 对应版本的 changelog；
  - 必要时更新 `README.md`、`优化版使用说明.md`。

### 二、发布前验证

- 修改完成后必须执行并记录结果：

  ```powershell
  python -m unittest discover -s tests -v
  python -m compileall -q token_manager tests
  git diff --check
  ```

- GUI测试必须使用临时目录、合成账号和模拟接口，禁止载入生产 Tokens、DPAPI 资料、真实密码、TOTP 密钥或真实 OAuth Token。
- 发布前必须确认更新流程不会修改 `tokens/`、`outputs/`、`token_manager_config.json`、DPAPI 凭据库或用户自定义配置。
- 不得为了验证自动恢复而人为制造生产账号 401、删除生产账号、刷新生产令牌或发起未授权模型请求。

### 三、本地打包与桌面替换

- 标准发布命令：

  ```powershell
  python build.py --name OpenAI-Token-Manager-88API --entry-point main.py
  ```

- 不要默认添加 `--clean`；构建脚本必须保留 `dist/` 中用户已有运行数据，除非用户明确要求清理。
- 标准打包会生成 `dist/OpenAI-Token-Manager-88API.exe`，并自动复制为：

  ```text
  C:\Users\Administrator\Desktop\88API-号池自动维护工具-v<版本>.exe
  ```

- 桌面替换只允许删除桌面根目录中匹配 `88API-号池自动维护工具-v*.exe` 的旧版本；禁止递归扫描、删除其他工具或删除用户文件。
- 如果旧版本正在运行，先通过正常窗口关闭请求让程序保存资料、停止维护和授权任务；不能强制结束正在写凭据的进程。旧版本无法安全退出时必须报告失败。
- 只生成 `dist`、不替换桌面时，必须显式使用：

  ```powershell
  python build.py --name OpenAI-Token-Manager-88API --entry-point main.py --no-desktop-install
  ```

### 四、固定云更新地址与清单

- 云更新地址固定为：

  ```text
  https://test.88api.ai/downloads/openai-token-manager/manifest.json
  ```

- 更新地址不得放入用户可编辑设置，不得回退到 GitHub Raw 或要求用户自行填写地址。
- 软件启动后自动检查更新；检查失败不得阻塞软件启动。当前有刷新、授权或自动维护任务时，更新提示必须等待任务结束，不得中断凭据写入。
- 更新清单必须由 `tools/create_update_manifest.py` 生成，至少包含：
  - `version`；
  - `download_url`；
  - `size`；
  - `sha256`；
  - `notes`；
  - `mandatory`。
- `download_url` 和清单地址必须使用 HTTPS，并指向 `test.88api.ai` 的独立分发目录。
- 更新包必须是完整、可运行的 Windows EXE。软件端下载后必须校验文件大小和 SHA-256，任何校验失败都拒绝安装。
- 更新只替换程序 EXE；不得覆盖 Tokens、输出文件、配置、DPAPI 资料、用户账号状态或运行日志。
- 更新替换必须由独立更新进程完成：主程序先安全退出，更新进程再替换旧 EXE、按新版本文件名落地并启动新版本。

### 五、香港物理机分发规则

- 远程服务器唯一使用 SSH 配置中的别名 `香港物理机`，操作前必须读取 `C:\Users\Administrator\.ssh\config`，并验证：

  ```powershell
  ssh -o BatchMode=yes -o ConnectTimeout=10 "香港物理机" hostname
  ```

- 当前已确认主机名为 `DS3166`。不得绕过 SSH 别名硬编码 IP 登录。
- 固定持久分发目录：

  ```text
  /www/wwwroot/test.88api.ai/downloads/openai-token-manager/
  ```

- 临时上传文件、校验输出和中转文件只能放在：

  ```text
  /www/wwwroot/work/
  ```

- `work/` 是临时目录，不用于长期保存发布文件；发布完成后必须删除临时 EXE、临时清单和脚本。规范备份只能保存在：

  ```text
  /www/wwwroot/work/backups/openai-token-manager/<时间戳>/
  ```

- `test.88api.ai` 原有 Node 程序、源码、数据库、启动脚本、业务 API 和原有反向代理不得修改。云更新只允许使用独立静态目录和站点级静态 location。
- 已存在静态 location 时不得重复添加；如果只是更新版本，只替换清单和 EXE，不修改 Nginx 配置。
- aaPanel 服务器上，站点配置必须通过 aaPanel 官方文件保存/站点配置入口维护；不得绕过面板直接手工创建全局 Nginx 配置、独立未登记站点或全局反向代理。

### 六、远程发布顺序

每次发布必须按照以下顺序执行，不能颠倒：

1. 读取 SSH 配置并验证 `香港物理机` 连接。
2. 创建带时间戳的规范备份，备份当前 `manifest.json`、当前 EXE，以及如有变更的站点级 Nginx 配置。
3. 本地打包并生成清单。
4. 通过 `scp` 将 EXE 和清单上传到 `/www/wwwroot/work/`。
5. 先把新 EXE 安装到持久分发目录。
6. 校验远程 EXE 大小和 SHA-256。
7. 确认 EXE 可下载后，最后替换 `manifest.json`。
8. 执行 `nginx -t`；语法检查失败时不得重载或重启 Nginx。
9. 验证清单 HTTP 200、EXE HTTP 200、清单版本、文件大小和 SHA-256 一致。
10. 验证 `https://test.88api.ai/` 原业务入口仍返回正常状态，并确认原 Node 进程未被意外重启。
11. 清理 `/www/wwwroot/work/` 中本次上传的临时文件，只保留规范备份。

- 发布时不需要 GitHub；`test.88api.ai` 是客户端唯一更新源。
- 更新清单必须最后发布，避免客户端读取到已经指向新版本但新 EXE 尚未上传完成的清单。
- 发布失败或验证不通过时，立即停止，不声称已发布成功。

### 七、回滚与失败处理

- 如果新版本存在严重问题，先从规范备份恢复 EXE，再恢复 `manifest.json`；清单必须最后恢复。
- 发布后不直接把版本号降回旧版本。修复应发布更高版本，例如 `2.3.1` 有问题时发布 `2.3.2`，避免客户端版本比较阻止更新。
- 如果更新进程无法关闭旧程序、替换失败、SHA-256 不一致、远程下载失败或 Nginx 检查失败，必须保留失败原因和备份路径。
- 未经用户明确授权，不启用强制更新（`mandatory=true`），不发布全量生产自动恢复，不把真实凭据写入清单、日志、测试或打包产物。

## 2.3.1：账号级联清理、导出与代理设置

- 左侧删除账号是受保护的级联操作：先读取当前 Sub2API 远端列表，按邮箱/稳定工作区身份唯一匹配；远端删除成功后才清理本地 Tokens 和 DPAPI 2FA 资料。远端读取失败、匹配不唯一或删除失败时保留本地记录，并将保留原因写入软件日志；不得用缓存列表执行破坏性远端删除。
- “导出为文件”输出单账号 Sub2API JSON，不得包含 `proxy_id` 或代理对象；不要把本机软件接口代理、OAuth授权代理或远端代理凭据写入导出文件。
- Sub2API 管理接口只使用管理员 API Key。旧版 `auth_mode`、管理员邮箱/密码、`access_token`、`refresh_token` 和 `token_expires_at` 配置必须迁移清理，不能再回退到密码登录。
- “OAuth授权代理（仅OAuth/2FA）”支持多行、逗号或分号分隔的 HTTP/HTTPS/SOCKS5/SOCKS5H 地址；软件接口代理只用于 Sub2API 管理接口，远端账号 `proxy_id` 不得复用为本地授权出口。浏览器链传给 Chromium 时将 `socks5h` 映射为 Chromium 支持的 `socks5`，协议链继续保留 `socks5h` 的代理端 DNS 语义。
- 上传参数和自动维护设置使用可滚动内容区，底部保存/取消操作必须固定可见；主界面和动态设置窗口的按钮、输入框、下拉框、文本区必须提供悬停说明。

## 2.3.2：速刷号购买入口与Sub2API刷新说明

- 主界面左侧操作区和顶部工作区导航中的“导入 / 导出”入口改为“速刷号购买”。
- 速刷号购买地址固定为 `https://catfk.com/shop/C96JR8SD`，不得放入用户可编辑设置；点击只打开浏览器，不上传本地账号或凭据。
- 内部 Sub2API 预览、复制和“导出为文件”能力继续保留，避免破坏已有同步与导出流程；内部页面不作为主导航入口。
- “刷新列表”与“更新用量”不是同一个功能：刷新列表重新读取远端账号、状态、分组、调度和服务器已有额度快照；更新用量调用官方用量接口刷新额度统计，通常只处理选中或当前筛选账号。需要同时更新时先刷新列表，再按需要更新用量。

## 2.3.3：左侧状态列简化

- 左侧“上传状态”只显示`已上传 #远端账号ID`或`未上传`，列内容居中；不得把“凭据已隐藏”“凭据不同”等诊断信息拼接到短状态中。
- 左侧“Sub2API监控”只显示`监控中`或`未监控`，列内容居中；自动维护的详细阶段、错误和恢复原因继续在账号详情与日志中展示。

## 2.3.4：本地移除并保留远端

- 左侧凭证区右键菜单的“移除账号（保留远端）”只删除本地 Token 文件和 DPAPI 加密2FA资料，不读取远端列表，不调用 Sub2API 删除接口，也不刷新或修改远端账号。
- 移除前必须确认，并阻止在自动维护或其他任务运行时执行；完成后写入日志、刷新左侧列表，并清理2FA编辑框对应行，防止退出保存时复活已删除资料。
- 原“删除账号”继续执行受保护的本地、2FA和唯一匹配远端级联删除；两个操作的文案必须保持明显区分。

## 2.3.5：授权代理与Sub2API界面精简

- OAuth/2FA授权代理只能通过“设置 → 基础”中位于“检查更新”前的“授权代理设置”按钮维护；独立窗口必须提供可换行的多行编辑区，不得恢复单行输入框。
- 内部预览固定输出Sub2API格式，不再提供预览格式标签、变量或下拉菜单；“导出为文件”仍须移除所有代理字段。
- 主设置页不再显示`Group IDs`；上传分组只在“Sub2API 设置 → 上传配置”中以“上传分组”字段维护，远端列表默认分组筛选仍保持独立。
- Sub2API账号列表顶部不再提供“账号操作”菜单；启停调度、刷新令牌、编辑和删除等账号操作统一保留在右键菜单。
- 上传分组与代理多选窗口不再提供“全选代理”和“清空代理（直连）”快捷按钮；直连通过取消全部代理勾选实现。

## 2.3.6：紧凑列表、居中启动与上传下拉菜单

- 主窗口启动前先完成布局，并按Windows可用工作区而非完整屏幕区域居中，避免任务栏导致窗口偏下；默认双栏在常规宽度下本地凭据区占54%，窄窗口下降为52%以防右侧导航重叠。
- 本地凭据列表的状态、7d额度、剩余时间和上传状态等短字段使用紧凑固定宽度，监控列标题简化为“监控”并保持默认可见；邮箱列继续弹性伸缩。
- 打开Sub2API上传参数窗口后自动读取远端OpenAI分组和活动代理，直接填充“添加到分组”和“账号是否使用代理”多选下拉菜单；不得恢复手动“读取分组 / 代理”按钮或独立选择弹窗。
- 不选择代理表示直连；选择一个或多个代理继续沿用每账号随机绑定一个代理且重复上传保留池内原代理的规则。
- “账号到期时自动暂停”使用只读“是/否”下拉框。自动维护四项布尔规则使用明确的蓝色勾选控件，不再显示系统主题的X形选中标记。

## 2.3.7：固定双栏与维护规则核对

- 移除“远端 / 双栏”按钮及隐藏左侧本地凭据栏的功能；主界面始终保留本地凭据与右侧工作区，避免误操作后找不到本地账号。
- “维护规则”必须直接来源于当前代码行为，并显示当前配置的检查周期与提前刷新阈值；规则窗口使用可滚动正文，完整说明监控范围、401分类、身份校验、代理边界、写回边界、调度、模型测试、重试和停止语义。
- 自动纳入维护只接受本地已有成功 Sub2API 上传记录且能唯一匹配远端的账号；仅有同邮箱远端记录但没有成功上传历史时不得自动纳入，仍可手动“监控选中”。
- 即使当前没有手动监控账号，只要存在符合自动纳入条件的账号，自动维护循环也必须执行远端核对，不得因启动门槛而漏掉首次自动纳入。
- `auto_enable_schedulable` 只在补授权恢复或待验证阶段开启调度；普通健康轮询不修改人工关闭的 schedulable。远端 `status=inactive`、普通429、非401错误和临时冷却均不触发重新登录。

## 2.3.8：2FA凭据操作行

- 2FA页的凭据资料操作固定为单行四按钮，顺序为“添加到左侧凭据、加密保存资料、载入已存资料、管理已存资料”；“添加到左侧凭据”保持主按钮样式。
- 当前授权模式/授权代理提示放在四按钮下方左侧，与“已加密保存 N 个账号”状态处于同一行；不得再让模式提示单独占用按钮上方一整行。

## 2.3.9：维护反馈与移除软件接口代理

- “维护规则”正文底部必须显示以下反馈文字：

  ```text
  有好的功能建议可以反馈，会考虑添加进去。
  VX：blackdm
  技术交流群：1004036018
  ```
- 删除“软件接口代理”设置、输入框和旧配置使用路径。Sub2API 管理接口、列表、上传、删除、用量、账号操作和自动维护 API 请求固定直连。
- `http_proxy` 仅可作为授权工具内部接收“授权代理”的兼容字段，由 `authorization_settings()` 临时注入；不得把它作为用户配置项，也不得从旧配置恢复。
- OAuth/2FA、本地令牌刷新和401重新授权继续使用 `auth_proxy`；Sub2API 远端账号 `proxy_id` 继续只负责远端业务出口，不能复用为本地授权代理。
- 保存旧配置时必须清理顶层 `http_proxy`；不得删除用户的 Tokens、输出目录、DPAPI 资料或历史文件。

## 2.3.10：维护反馈联系方式补充

- “维护规则”正文底部在 VX 联系方式后继续显示：

  ```text
  技术交流群：1004036018
  ```
