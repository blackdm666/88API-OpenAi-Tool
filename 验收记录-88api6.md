# 2.2.0-88api.6

2026-09-18，用户要求去除冗余账号详情、对齐Sub2API排序、展示额度用量、整理右侧布局并默认OAuth。

- 右侧列表改为单层工具栏/对齐筛选/表格/简短页脚；移除嵌套远端/失效页签和身份详情文本，账号操作在菜单。失效账号仍可通过状态筛选查看。
- 初始和重置筛选均oauth；其他类型只在用户明确切换后出现。
- 列表API显式name/asc并保留响应顺序。左侧本地账号在远端刷新后按唯一匹配的远端顺序排列，未匹配排后。不读取浏览器保存的自定义排序。
- 左右表格显示5h、7d已用百分比，读取账号extra中已有codex快照。缺失/错误/过期分别为暂无/读取失败/待更新；窗口过期不沿用旧百分比。
- 用量明细调用官方usage/batch，force=false，每批20、每次最多50。服务器可能按自己的规则刷新旧快照；本客户端不会定时请求全量账号用量。API窗口统计占位0不当作已知0%配额。
- 双击账号可查看底部用量明细，包括重置时间、窗口请求数、Token和账号费用。页面底部不显示重复邮箱/名称。

验证：36项测试通过、改动文件F/E9 lint、compileall、git diff --check通过。生产官方管理API读取178条OpenAI记录、175条OAuth，前3条服务端排序与客户端顺序相同；175条OAuth均有未过期7d快照。另3账号官方usage/batch返回完整5h/7d/重置/窗口统计，未强制探测。没有修改生产账号配置或凭据。

已在桌面替换并启动2.2.0-88api.6；实际窗口确认默认oauth、两侧用量列和简化工具栏，无列表底部详情框。尝试在窗口刷新时检测到用户操作，随后窗口被最小化，未继续抢占焦点或操作。

旧桌面EXE和配置备份：C:\Users\Administrator\AppData\Local\OpenAI-Token-Manager\backups\20260918_110742-usage-layout\。

新EXE SHA256：1994bb49d74c5a4bf6b5a98f36edbade7ffc4edbea55f79f4b18a742af2fb793。

建议复盘：刷新列表后对照Sub2API相同分组/类型和名称升序；选中少量账号更新用量，对照百分比和重置时间。快照反映上次采样，不等于每次刷新都直接访问OpenAI。
