# 2.2.0-88api.2 验收

2026-09-18，codex/functional-improvements。

- CPA：删除gui_cpa.py及相应服务、API、Docker文件操作、转换/伪造兼容id_token生成和界面配置；运行模块仅保留旧配置迁移时移除cpa键。用户桌面历史CPA目录没有删除。
- WS：使用当前Sub2API源码EditAccountModal契约，OAuth专属字段openai_oauth_responses_websockets_v2_mode=ctx_pool、enabled=true；上传时应用。自动恢复不改远端设置。
- 分组：上传选项、远端筛选均支持复选框多选；筛选为OR语义。
- 下拉框：补齐readonly selected foreground和原生弹出Listbox颜色；Windows实际窗口验证WS四项清晰可见。
- 2FA：分析协议链42份单账号报告，其中24成功、12缺失会话、4Cloudflare拦截、2无orgs。缺失会话12份的MFA都HTTP200且JSON含oai-client-auth-session.workspaces。新解析器12/12离线处理成功。浏览器链另见2次调试端口连接失败和1次无orgs。日志中的用户名、会话内容、验证码没有复制到仓库。
- 修复JSON会话读取、浏览器无组织直达分支、独立profile与浏览器提前退出诊断；TOTP本地RFC6238生成、窗口剩余不足5秒时等下一周期。Cloudflare不绕过，提供官方交互登录提示。
- 25项测试通过，包含RFC6238六组官方向量、MFA返回兼容、无orgs分支、退出进程、分组多选、WS参数及原19项恢复/界面回归；compileall、改动文件F/E9 lint、git diff --check通过。
- 打包通过，EXE版本入口退出码0；桌面实际窗口标题2.2.0-88api.2。GUI经官方管理GET读取6个分组、4个代理，展示4个OpenAI上传分组；WS下拉与复选框实际视觉核验通过。没有执行真实2FA登录，也没有刷新/修改生产账号。
- 桌面旧EXE和配置备份：C:\Users\Administrator\AppData\Local\OpenAI-Token-Manager\backups\20260918_100603-cpa-re6oval\。
- 桌面与dist可执行文件SHA256：74dd0caa96ff0b59bc697404288769aae9870b1934faa4f5f399469dd2a5f68d。
- 生产服务器文件、宝塔/Nginx/Docker均无修改；此次是Windows客户端更新。现有tokens目录、监控登记保留，自动维护未自行启动。

需要复盘：真实账号完整授权在当前网络中的成功率；Cloudflare/代理出口变化和账号安全策略可能继续要求人工操作，离线兼容通过不代表全部授权失败已消失。
