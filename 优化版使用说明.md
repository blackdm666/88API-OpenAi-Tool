# 2.2.0-88api.8

## 最新显示与刷新规则

- 默认按ID数值升序，小ID在上；点击右侧任意表头可切换升降序，箭头表示当前排序。额度列按百分比数字排序，缺失项排后。本地已匹配账号跟随远端当前排序。
- 日志默认常显，底部区域全部用于日志正文，“清空日志”浮于右上角；不再有用量明细页签或展开收起按钮。维护规则、远端/双栏切换与设置导航处于同一行。账号统计显示于左侧“账号列表”标题旁。
- 额度以绿/橙/红进度条展示已用比例，暂无、读取失败和待更新保留明确文字。
- 额度不是直连OpenAI实时推送：已配置连接时启动自动加载；之后每60秒GET Sub2API账号列表刷新已有快照，右侧显示最近同步时间。自动维护运行时复用维护轮询，不重复同步。服务端快照未变时，客户端数字也可能不变。
- “更新用量”仍是手动动作，会调用Sub2API官方用量接口。下面历史版的名称默认排序、用量明细及日志收起说明已由本节替代。

## 账号列表与用量

右侧默认筛选OAuth账号，重置筛选后仍为OAuth。列表按Sub2API默认的名称升序请求并保留服务器顺序；左侧已匹配账号在刷新远端列表后跟随相同顺序，未匹配账号排后。若浏览器中手动保存了其他排序，本工具不会读取浏览器私有排序偏好。

两侧新增“5h已用”“7d已用”列，先展示Sub2API账号已有的配额快照。选中远端账号点击“更新用量”，未选择时更新当前筛选结果（最多50账号，分批20），调用官方usage/batch、force=false。服务器可能按自身规则刷新过期快照；这不是客户端全量高频探测。

双击远端账号，在底部“用量明细”查看重置时间、窗口请求数、Token用量和账号费用。百分比代表已用额度，不是剩余Token或精确剩余次数。暂无、待更新、读取失败分开显示；窗口已过期不继续展示旧百分比。列表下不再显示邮箱、名称等重复详情；启停/删除/刷新凭据移入“账号操作”菜单。

## 代理池与紧凑界面

“上传配置 → 多选分组 / 多选代理”可同时勾选多个代理，也可在代理池ID中填 `8,9,10`。每个账号上传时随机选一个：已有远端账号使用池内代理时保留原代理，新分配结果保存在本地账号记录里，重试不重新抽选。移除某个候选代理后，下次手动上传才重新分配；保存设置本身不会修改远端账号。留空或单独填0表示直连，不能把0混入代理池。随机分配不保证每个账号使用不同出口，也不等于每次请求轮换IP。401自动恢复继续保留远端原代理。

大标题栏已移除。底部“运行信息”集中显示状态、日志开关、维护规则、双栏切换及清空日志；下面的页签分别是运行日志、远端账号详情和失效账号详情。选中远端账号可在信息区查看详情，列表下方不再预留空白详情框。默认左右各50%，信息区可拖动高度或收起内容。

本轮已移除CPA页面、连接配置、远端API、上传目标和导出模块；保留原有磁盘历史文件。程序聚焦本地账号与Sub2API。

上传参数新增WS mode，默认“上下文池（ctx_pool）”，提交OAuth专属mode与enabled字段。关闭/透传/HTTP桥接也可选择。账号级选择不代替Sub2API服务端全局WS配置。已有账号仅在用户执行上传时应用，自动恢复保持原远端配置。

上传分组及远端列表分组筛选均使用复选框多选。上传配置中点“多选分组 / 选择代理…”读取后台选项；筛选分组时不选任何项表示全部。多个筛选分组为“或”的关系。

下拉框显式设置只读文本、选择文本、弹出列表与选中行颜色，避免系统主题导致白底白字。

2FA故障修复：12份历史失败报告的MFA已200成功，但工作区放在JSON的oai-client-auth-session中；现兼容该返回与旧Cookie。浏览器链支持无orgs但有continue_url的正常分支。每次授权使用独立浏览器profile，浏览器进程退出时及时报告，不一直等端口超时。TOTP改为本机RFC6238生成，剩余不足5秒时等下一周期；需保持系统时间同步。

Cloudflare拦截不自动绕过、不通过盲目重试处理；请转“授权”页完成官方交互登录。本次回归包含历史响应离线重放，没有用真实账号再次登录，因此不保证所有网络/风控导致的失败都已消失。

这是OpenAI Token Manager的第一轮功能优化，保留原有Tkinter程序和账号目录兼容。

## 列表与界面

运行日志默认收起，顶部可以展开；“专注远端 / 双栏”可以把远端账号池切为全宽。Sub2API列表默认展示邮箱、分组、状态和错误摘要，其他信息在可展开的详情区域查看。刷新时回到最左列，避免横向滚动停留在错误信息一侧。分页兼容只有total而未返回pages的服务端。

## 连接与上传参数

点击左侧或Sub2API页的“上传配置”。服务器只填域名时自动补HTTPS；常见的/api/v1、登录路径自动归一到服务根地址。鉴权方式auto优先使用管理Key；api_key不回退到旧邮箱密码；password使用邮箱密码登录。

上传可设置并发、调度优先级、倍率、IP代理、分组、到期自动暂停及Codex设备指纹收敛（off/device/session/full）。点击“读取分组 / 代理”可选择实际名称与ID。直连填空或0。该代理是Sub2API账号出站代理，与软件“全局代理”不同。

**手动上传会应用保存的上传参数。** 已有唯一匹配的远端账号会更新原账号；同邮箱多个账号或工作区冲突时停止，不随意选择或创建重复项。自动恢复仅更新凭据，保留远端业务参数。

## 401自动恢复

1. 配置Sub2API连接并刷新远端列表。
2. 在左侧选中已添加的本地账号，点击“监控选中”。程序核对唯一身份并绑定当前服务器及远端账号ID。
3. 点击“启动自动维护”。只监控显式开启的账号，不会因为筛选条件改变而扩大范围。
4. 默认每60秒读取远端状态：error状态且明确401时尝试OAuth刷新，保存新令牌，再通过官方apply-oauth-credentials接口写回原账号、清理错误并读回校验。
5. 普通429、非401错误、临时停调度、手工停用不触发这条恢复链。服务器自己的临时401恢复仍由服务器负责。
6. 恢复失败默认至少等待5分钟，后续10/20分钟退避，最多3次。上传阶段失败时保留新令牌，下次只续传。若远端凭据被其他任务修改则停止，显示“凭据冲突”。
7. token_revoked、token_invalidated、invalid_grant等不可恢复授权会显示“需要重新授权”。停止维护，在授权页完成官方登录并保存同一邮箱账号，再启动维护，程序会同步新凭据。刷新接口不能凭空恢复被撤销的授权。

左侧“Sub2API监控”列显示状态。维护规则按钮可随时查看说明。停止按钮会等待当前网络请求返回后停止后续动作；关闭窗口时先安全退出维护，防止新refresh token尚未保存就退出。

## 原自动维护规则

旧版默认每60秒，仅刷新本地未过期且剩余时间≤300秒的账号，不查Sub2API也不上传。新版保留此行为用于未开启远端监控的账号；已监控账号统一交给恢复循环，避免两处同时旋转令牌。程序必须保持运行；本版本没有安装后台服务或计划任务。

## 验证与边界

单元/集成模拟覆盖鉴权、分页、配置、身份冲突、原账号更新、重试、撤销、并发旋转和停止语义；Tk实际组件测试检查不同窗口尺寸、表格可见行、切换与设置弹窗。生产只读测试读取175个唯一OpenAI账号、6个分组、4个代理；没有在生产刷新、上传、禁用或删除账号。

上线前建议先选一个自有账号小范围复盘：列表正常 → 参数保存 → 监控状态可见 → 真实异常恢复或转人工授权。恢复写入已通过模拟API回归，尚未用真实失效账号进行端到端验收。不保证用户机器的全部DPI缩放/字体和所有上游授权状态均已覆盖。

- 2.2.0-88api.9：任务忙碌或自动维护运行时，重复点击刷新等操作仅记录日志提示，不再弹出阻塞提示框；禁止在任务锁内调用Tk弹窗或其他UI回调，避免定时器重入死锁。


## 2.2.0-88api.10：401 自动维护完整流程

1. 在 2FA 页导入账号资料，点击“加密保存资料”。导入、启动授权和正常退出也会保存。Windows 实际文档目录（包括重定向到 OneDrive 的文档）下 `OpenAI-Token-Manager/credentials/accounts.dpapi` 保存账号、密码和 TOTP 密匙；仅当前 Windows 用户可解密。清空输入框不删除资料库，启动只显示已保存数量，“载入已存资料”才显示完整资料。旧版本未保存的资料需导入一次。
2. 打开右侧 `Sub2API 设置 → 自动维护`：开启 401 自动重新授权和恢复后模型测试，默认测试模型 `gpt-5.5`，可以直接修改。也可从 `设置 → Sub2API` 进入。
3. 左侧选择需要维护的账号，点击“监控选中”，确认绑定后点击“启动自动维护”。只有已监控账号会恢复；关闭程序停止维护，重启需再次点击启动。
4. 每轮从 Sub2API 读取账号：核对原 ID、邮箱和工作区，确认远端已存在、凭据与当前状态。明确401时先刷新；不可刷新的授权用资料库重新登录。新授权必须通过身份、完整性和有效期检查。
5. 新凭据先持久化，再补到 Sub2API 原账号，读回确认凭据和启用状态；分组、代理、并发、设备指纹保持原值。网络失败只重试补授权，不重复登录；发现凭据被别的任务修改时停止覆盖。
6. 启用和调度检查通过后，调用 Sub2API 原生账号测试。只有收到明确测试成功才显示完整恢复；结果包含模型与错误原因，可在左侧“详情”查看。每份新凭据只自动测试一次，普通60秒轮询不消耗模型额度。冷却/停调度先等待，测试429不会被当作需要重新登录。
7. 缺失资料、验证码/登录挑战、身份不符、连续失败3次或一小时重新授权3次后交给人工。主动停用账号保持停用，不强行启用。补授权API恢复error账号后仍须读回active才能继续。

左侧“未过期”仅代表本地期限；“授权失效/待补授权/可调度”等来自远端快照。“已上传 #ID · 已同步/凭据不同”展示当前匹配及凭据关系，不再用历史上传OK代替当前可用性。

- 生产接口注意：列表与详情会隐藏OAuth Token。普通轮询显示“凭据已隐藏”，不误报不一致；仅恢复阶段通过官方GET accounts/data?ids=<单账号ID>&include_proxies=false读取凭据，在内存校验身份/并发变化/补授权读回，不落盘导出文件。接口403时停止恢复，不绕过二次验证。实测当前管理员接口可用。

- Sub2API列表默认筛选分组ID2（精确按ID，不依赖分组名称）。在“Sub2API设置→列表显示”编辑默认列表分组ID，支持逗号分隔多选，留空显示全部；启动、重置筛选和修改保存后应用。临时多选不改变默认配置。此字段default_list_group_ids独立于上传group_ids及监控范围。

- 2.2.0-88api.11：Sub2API列表在状态右侧增加“调度”列，可点击排序。依据schedulable、status及冷却截止时间显示参与调度/已关闭/开启·账号停用或异常/开启·临时、限流、过载冷却；未知不当作开启。调度表示服务器快照中的参与资格，不代表当前正在处理请求；随列表轮询更新，不修改远端调度开关。

- 2.2.0-88api.12：自动维护默认纳入已有Sub2API成功上传且唯一匹配的本地账号，不再要求先点击监控选中；“取消监控”写入manual_disabled，保持人工排除。恢复或检查active账号时，若schedulable=false且auto_enable_schedulable开启，调用官方POST /accounts/:id/schedulable true并读取确认，再进行模型测试。调度未知不当作参与调度。

- 2.2.0-88api.13：Sub2API账号操作菜单只提供“启用调度/停用调度”，调用官方POST /accounts/:id/schedulable；不再使用bulk-update status。status=active/inactive仅由账号状态操作或服务端返回决定。核对#333、#342时官方列表与详情均为active、schedulable=true、无冷却。

- 2.2.0-88api.14：右侧Sub2API列表使用官方accounts?lite=1读取current_concurrency和concurrency，每5秒同步；current_concurrency是当前请求占用计数，不等同active_sessions（OpenAI通常为空）。额度展示移除5h，仅保留7d。左侧身份标签按Sub2API plan_type：Plus、Pro 20x、Pro 5x、Business Standard、Business Premium、Free、Enterprise、Unknown。

- 2.2.0-88api.15：右侧Sub2API列表原“分组”列改为“账号标签”，标签按OpenAI plan_type定义并优先使用本地匹配账号的Sub2API标签。账号操作菜单统一称“刷新令牌（选中）”；该动作调用Sub2API POST /api/v1/admin/accounts/batch-refresh，刷新远端OAuth令牌，不是刷新列表或额度。

- 2.2.0-88api.16：Sub2API列表账号行支持右键菜单，右键行会自动选中，提供启用调度、停用调度、刷新令牌、删除选中；刷新令牌调用官方batch-refresh接口。


## 2.2.0-88api.17：智能补授权与2FA资料删除

- 2FA页的“启动批量授权”改为“智能补授权”。每次点击后在后台重新读取Sub2API账号列表，按邮箱/工作区/绑定ID唯一匹配；远端active且没有明确缺失凭据时跳过。调度关闭、额度冷却不等于授权失效。inactive、非401异常或身份歧义跳过并记录原因；网络读取失败终止本批，不回退全量登录。
- 明确401/授权撤销/缺少凭据或首次添加的账号才进入授权。已有未过期新凭据处于待上传/待验证阶段时提示继续同步，不重复授权。未匹配远端但已有本地未过期凭据时先提示核对上传。状态是检查时刻的Sub2API快照，不是逐账号真实模型探测。
- 结果展示成功、失败、跳过数量，每个跳过原因写入日志。正常账号不会调用登录、MFA或刷新令牌接口。首次使用需先配置Sub2API连接，以便进行状态核对。
- 删除入口：2FA → 管理已存资料 → 搜索邮箱/多选 → 删除所选资料 → 确认。列表只显示邮箱，不展示密码或密匙。删除使用DPAPI密文原子替换，保留其他账号；同时移除输入框对应行，避免退出时再次保存。任务或维护运行时禁止删除资料；不会删除本地Token文件或Sub2API账号。若需要以后重新授权被删账号，须重新导入资料。
- “清空输入”只清空编辑框，与删除资料库是两个动作。不要自动删除用户的生产账号；回归删除测试仅使用临时资料库。

## 2.2.0-88api.18：远端账号重建后的自动重绑

如果 Sub2API 删除并重建了同一个 OAuth 账号，导致远端 ID 变化，自动维护会在邮箱和 ChatGPT 工作区身份都唯一一致、且旧绑定 ID 已不存在时更新本地绑定，然后继续 401 重新授权、原账号补凭据和模型测试。手动上传仍要求绑定严格一致；工作区不一致、重复账号或旧绑定仍存在时不会自动选择。

## 2.2.0-88api.19：统一刷新令牌文案

Sub2API 账号操作菜单和右键菜单统一显示“刷新令牌”，仍只对当前选中的账号调用远端 OAuth 令牌刷新接口。

## 2.2.0-88api.20：精简左侧账号操作

左侧移除“刷新当前”“上传当前”和“取消监控”按钮，避免与选中操作重复并减少占用空间。本地账号右键菜单保留“刷新本地令牌”“上传到 Sub2API”“监控选中”“取消监控”和“删除账号”。
