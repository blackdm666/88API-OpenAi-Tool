# 2.2.0-88api.5

## 代理池与紧凑界面

“上传配置 → 多选分组 / 多选代理”可同时勾选多个代理，也可在代理池ID中填 `8,9,10`。每个账号上传时随机选一个：已有远端账号使用池内代理时保留原代理，新分配结果保存在本地账号记录里，重试不重新抽选。移除某个候选代理后，下次手动上传才重新分配；保存设置本身不会修改远端账号。留空或单独填0表示直连，不能把0混入代理池。随机分配不保证每个账号使用不同出口，也不等于每次请求轮换IP。401自动恢复继续保留远端原代理。

大标题栏已移除。底部“运行信息”集中显示状态、日志开关、维护规则、双栏切换及清空日志；下面的页签分别是运行日志、远端账号详情和失效账号详情。选中远端账号可在信息区查看详情，列表下方不再预留空白详情框。默认左右各50%，信息区可拖动高度或收起内容。

本轮已移除CPA页面、连接配置、远端API、上传目标和导出模块；保留原有磁盘历史文件。程序聚焦本地账号与Sub2API。

上传参数新增WS mode，默认“上下文池（ctx_pool）”，提交OAuth专属mode与enabled字段。关闭/透传/HTTP桥接也可选择。账号级选择不代替Sub2API服务端全局WS配置。已有账号仅在用户执行上传时应用，自动恢复保持原远端配置。

上传分组及远端列表分组筛选均使用复选框多选。上传配置中点“多选分组 / 选择代理…”读取后台选项；筛选分组时不选任何项表示全部。多个筛选分组为“或”的关系。

下拉框显式设置只读文本、选择文本、弹出列表与选中行颜色，避免系统主题导致白底白字。

2FA故障修复：12份历史失败报告的MFA已200成功，但工作区放在JSON的oai-client-auth-session中；现兼容该返回与旧Cookie。浏览器链支持无orgs但有continue_url的正常分支。每次授权使用独立浏览器profile，浏览器进程退出时及时报告，不一直等端口超时。TOTP改为本机RFC6238生成，剩余不足5秒时等下一周期；需保持系统时间同步。

Cloudflare拦截不自动绕过、不通过盲目重试处理；请转“授权”页完成官方交互登录。本次回归包含历史响应离线重放，没有用真实账号再次登录，因此不保证所有网络/风控导致的失败都已消失。

这是OpenAI Token Manager的第一轮功能优化，保留原有Tkinter程序和账号目录兼容。

## 列表与界面

运行日志默认收起，顶部可以展开；“专注远端 / 双栏”可以把远端账号池切为全宽。Sub2API列表默认展示邮箱、分组、状态和错误摘要，其他信息在可展开的详情区域查看。刷新时回到最左列，避免横向滚动停留在错误信息一侧。分页兼容只有total而未返回pages的服务端。

## 连接与上传参数

点击左侧或Sub2API页的“上传配置”。服务器只填域名时自动补HTTPS；常见的/api/v1、登录路径自动归一到服务根地址。鉴权方式auto优先使用管理Key；api_key不回退到旧邮箱密码；password使用邮箱密码登录。

上传可设置并发、调度优先级、倍率、IP代理、分组、到期自动暂停及Codex设备指纹收敛（off/device/session/full）。点击“读取分组 / 代理”可选择实际名称与ID。直连填空或0。该代理是Sub2API账号出站代理，与软件“全局代理”不同。

**手动上传会应用保存的上传参数。** 已有唯一匹配的远端账号会更新原账号；同邮箱多个账号或工作区冲突时停止，不随意选择或创建重复项。自动恢复仅更新凭据，保留远端业务参数。

## 401自动恢复

1. 配置Sub2API连接并刷新远端列表。
2. 在左侧选中已添加的本地账号，点击“监控选中”。程序核对唯一身份并绑定当前服务器及远端账号ID。
3. 点击“启动自动维护”。只监控显式开启的账号，不会因为筛选条件改变而扩大范围。
4. 默认每60秒读取远端状态：error状态且明确401时尝试OAuth刷新，保存新令牌，再通过官方apply-oauth-credentials接口写回原账号、清理错误并读回校验。
5. 普通429、非401错误、临时停调度、手工停用不触发这条恢复链。服务器自己的临时401恢复仍由服务器负责。
6. 恢复失败默认至少等待5分钟，后续10/20分钟退避，最多3次。上传阶段失败时保留新令牌，下次只续传。若远端凭据被其他任务修改则停止，显示“凭据冲突”。
7. token_revoked、token_invalidated、invalid_grant等不可恢复授权会显示“需要重新授权”。停止维护，在授权页完成官方登录并保存同一邮箱账号，再启动维护，程序会同步新凭据。刷新接口不能凭空恢复被撤销的授权。

左侧“Sub2API监控”列显示状态。维护规则按钮可随时查看说明。停止按钮会等待当前网络请求返回后停止后续动作；关闭窗口时先安全退出维护，防止新refresh token尚未保存就退出。

## 原自动维护规则

旧版默认每60秒，仅刷新本地未过期且剩余时间≤300秒的账号，不查Sub2API也不上传。新版保留此行为用于未开启远端监控的账号；已监控账号统一交给恢复循环，避免两处同时旋转令牌。程序必须保持运行；本版本没有安装后台服务或计划任务。

## 验证与边界

单元/集成模拟覆盖鉴权、分页、配置、身份冲突、原账号更新、重试、撤销、并发旋转和停止语义；Tk实际组件测试检查不同窗口尺寸、表格可见行、切换与设置弹窗。生产只读测试读取175个唯一OpenAI账号、6个分组、4个代理；没有在生产刷新、上传、禁用或删除账号。

上线前建议先选一个自有账号小范围复盘：列表正常 → 参数保存 → 监控状态可见 → 真实异常恢复或转人工授权。恢复写入已通过模拟API回归，尚未用真实失效账号进行端到端验收。不保证用户机器的全部DPI缩放/字体和所有上游授权状态均已覆盖。
